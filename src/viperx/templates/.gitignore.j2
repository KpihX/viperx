# =============================================================================
# 🚫 .GITIGNORE - What NOT to Track in Git
# =============================================================================
#
# 🎓 WHY THIS FILE IS CRITICAL:
#
# Git tracks EVERYTHING by default. Without .gitignore, you'd commit:
# - 500MB of downloaded datasets
# - Your secret API keys (SECURITY RISK!)
# - Thousands of cache files
# - OS-specific junk (.DS_Store, Thumbs.db)
#
# Rule of thumb: If it can be REGENERATED or is PERSONAL, ignore it.
#
# 📚 Learn more: https://git-scm.com/docs/gitignore
# 📚 Templates: https://github.com/github/gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# 🐍 PYTHON - Generated Files
# -----------------------------------------------------------------------------
# 🎓 WHY: These are created when Python runs your code. They speed up imports
# but are machine-specific and can be regenerated anytime.

__pycache__/          # 🎓 Compiled Python bytecode (auto-generated)
*.py[cod]             # 🎓 .pyc, .pyo, .pyd files
*$py.class            # 🎓 Java-style compiled files
*.so                  # 🎓 Compiled C extensions

# -----------------------------------------------------------------------------
# 📦 PACKAGING - Build Artifacts
# -----------------------------------------------------------------------------
# 🎓 WHY: These are created when you build your package for distribution.
# They should be rebuilt fresh for each release.

build/                # 🎓 Build output directory
dist/                 # 🎓 Distribution files (.whl, .tar.gz)
*.egg-info/           # 🎓 Package metadata (generated by setuptools)
*.egg                 # 🎓 Old-style Python eggs

# -----------------------------------------------------------------------------
# 🔧 VIRTUAL ENVIRONMENTS
# -----------------------------------------------------------------------------
# 🎓 WHY: Your venv contains installed packages - often 100MB+!
# Anyone can recreate it with `uv sync` or `pip install -r requirements.txt`

.venv/                # 🎓 Virtual environment (ViperX/uv default)
venv/                 # 🎓 Alternative venv name
env/                  # 🎓 Another common name
ENV/                  # 🎓 Windows-style

# -----------------------------------------------------------------------------
# 🔐 SECRETS - NEVER COMMIT THESE!
# -----------------------------------------------------------------------------
# 🎓 WHY: API keys, passwords, tokens in git = SECURITY BREACH
# Use .env files locally, environment variables in production.
# 
# ⚠️ If you accidentally commit a secret:
# 1. Revoke the key IMMEDIATELY
# 2. Use `git filter-branch` or BFG to remove from history
# 3. Force push (coordinate with team)

.env                  # 🎓 Environment variables (YOUR SECRETS!)
.env.local            # 🎓 Local overrides
*.pem                 # 🎓 SSL certificates
*.key                 # 🎓 Private keys

# 🎓 NOTE: .env.example IS tracked - it's a template without real values
# Isolated .env files in package directories
src/**/.env           # 🎓 Package-isolated environment variables

# -----------------------------------------------------------------------------
# 📊 DATA - Large Files
# -----------------------------------------------------------------------------
# 🎓 WHY: Git is for CODE, not data. Large files:
# - Slow down clone/pull/push
# - Bloat repository size forever (git keeps history)
# - Often have licensing restrictions
#
# Solutions for large data:
# - Git LFS (for files you MUST track)
# - DVC (Data Version Control)
# - Cloud storage (S3, GCS) with download scripts

data/                 # 🎓 Datasets (can be 100MB-10GB+)
!data/.gitkeep        # 🎓 But keep the directory structure
*.csv                 # 🎓 Unless you NEED them in git
*.parquet
*.h5
*.hdf5
*.pkl                 # 🎓 Pickle files (also security risk!)
*.joblib

# -----------------------------------------------------------------------------
# 📓 JUPYTER NOTEBOOKS - Checkpoints
# -----------------------------------------------------------------------------
# 🎓 WHY: Jupyter auto-saves checkpoints. They're just backups.

.ipynb_checkpoints/

# -----------------------------------------------------------------------------
# 🖥️ IDE & EDITOR - Personal Preferences
# -----------------------------------------------------------------------------
# 🎓 WHY: Your IDE settings ≠ your teammate's IDE settings
# Everyone uses different editors, themes, plugins.

.idea/                # 🎓 PyCharm/IntelliJ
.vscode/              # 🎓 VS Code (some teams DO track this - your choice)
*.swp                 # 🎓 Vim swap files
*.swo
*~                    # 🎓 Emacs backup files
.spyderproject        # 🎓 Spyder
.spyproject

# -----------------------------------------------------------------------------
# 💻 OS - System Files
# -----------------------------------------------------------------------------
# 🎓 WHY: These are created by your operating system, not your project.

.DS_Store             # 🎓 macOS folder metadata
.DS_Store?
Thumbs.db             # 🎓 Windows image thumbnails
ehthumbs.db
Desktop.ini           # 🎓 Windows folder settings

# -----------------------------------------------------------------------------
# 🧪 TESTING & COVERAGE
# -----------------------------------------------------------------------------
# 🎓 WHY: Test outputs can be regenerated by running tests again.

.coverage             # 🎓 Coverage.py data file
htmlcov/              # 🎓 HTML coverage reports
.pytest_cache/        # 🎓 Pytest cache
.tox/                 # 🎓 Tox environments
.nox/                 # 🎓 Nox environments

# -----------------------------------------------------------------------------
# 📝 VIPERX & PROJECT-SPECIFIC
# -----------------------------------------------------------------------------
# 🎓 Your project-specific ignores can go here

{%- if project_type in ['ml', 'dl'] %}

# ML/DL specific ignores
models/               # 🎓 Trained model weights (often 100MB+)
checkpoints/          # 🎓 Training checkpoints
logs/                 # 🎓 TensorBoard/training logs
wandb/                # 🎓 Weights & Biases local data
mlruns/               # 🎓 MLflow tracking
{%- endif %}
